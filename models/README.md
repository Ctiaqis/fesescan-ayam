# Folder Model

Letakkan file model final di folder ini dengan nama:

```text
efficientnetb0_direct_multiclass.keras
```

File model tidak disertakan di template ini. Ambil dari hasil training notebook Direct Multiclass.
